namespace GuidedMentor.SharedInfrastructure.HealthChecks;

// Removed: DynamoDB health check no longer needed with PostgreSQL migration.
// PostgreSQL health is checked via AuroraHealthCheck (renamed but kept for backward compat).
